spring:
  application:
    name: AnalisysService
  datasource:
    url: jdbc:postgresql://db-users-api:5432/mydb
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        format_sql: true
file:
  url: http://storage-service:8080/api/files/{id}/url
server:
  port: 8081